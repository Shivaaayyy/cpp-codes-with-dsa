#include<iostream>
using namespace std;
int main()
//checking weather no is equal or not
{    int a  = 5;
    int b = 6;
    bool first= (a==b);
    cout <<first<< endl;
    bool second = (a<b);
    cout<<second<<endl;
    bool third = (a<=b);
    cout<<third<<endl;
    bool fourth=(a!=b);// ! indicates not equals to
    cout<<fourth<<endl;
    
        // 
}